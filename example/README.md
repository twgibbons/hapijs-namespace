# hapijs-namespace Sample

This simple example creates a hapi server and applies a single namespaced
route. As configured it listens on port 8080 and answers `/healthcheck/ping`
with `{ ping: 'PONG!' }`

## Install

    npm install
    
## Run

    npm start
